services:
  app:
    image: node:20-alpine
    working_dir: /app/CourseImageMaker
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
    command: sh -c "npm install && npm run dev -- --host"
    tty: true
